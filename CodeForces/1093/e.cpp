#include <bits/stdc++.h>
#define fl(i, m, n) for (int (i) = (m); (i) < (n); ++(i))

using namespace std;

typedef long long ll;
typedef long double ld;

//TODO use scanf instead
int main() {

	int n, m;
	cin >> n >> m;
	int a[n], b[n];
	fl (i, 0, n) cin >> a[i];
	fl (i, 0, n) cin >> b[i];

	while (m--) {
		int type;
		cin >> type;
		if (type == 1) {
			int la, ra, lb, rb;
			cin >> la >> ra >> lb >> rb;
			
		}
		else {
			int x, y;
			cin >> x >> y;
		}
	}

    return 0;
}
