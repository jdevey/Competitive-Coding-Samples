#include <bits/stdc++.h>

#define fl(i,m,n) for(int (i) = (m); (i) < (n); ++(i))

#ifndef ONLINE_JUDGE
#define main() main(){freopen("zin.txt", "r", stdin);
#endif

using namespace std;

typedef long long ll;

int main() {

	

    return 0;
}}
