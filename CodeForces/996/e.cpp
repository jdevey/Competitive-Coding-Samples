#include <bits/stdc++.h>

#define fl(i,m,n) for(int (i) = (m); (i) < (n); ++(i))

using namespace std;

typedef long long ll;

struct V {
	int x, y, sign;
	vector <pair <pair <int, int>, pair <int, int>> vpp;
	V(_x, _y, _i) : x(_x), y(_y), sign(1) {
		vpp.push_back({{_i, 1}, {_x, _y}});
	}
	void sw(
};

int main() {

#ifndef ONLINE_JUDGE
	freopen("zin.txt", "r", stdin);
#endif

	int n;
	cin >> n;
	pair <int, int> a[n];
	fl (i, 0, n) cin >> a[i].first >> a[i].second;



    return 0;
}
