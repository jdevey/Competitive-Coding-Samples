#include <bits/stdc++.h>

#define fl(i,m,n) for(int (i) = (m); (i) < (n); ++(i))

using namespace std;

typedef long long ll;

class Solution {
public:
    int XXXXXXXXXX(vector<int>& nums) {
        
    }
};

int main() {

#ifndef ONLINE_JUDGE
	freopen("zin.txt", "r", stdin);
#endif

	vector <int> nums;
	fl (i, 0, 10) {
		int a;
		cin >> a;
		nums.push_back(a);
	}

	Solution Sol;
	cout << Sol.XXXXXXXXXX(nums);

    return 0;
}
