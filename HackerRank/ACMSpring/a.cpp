#include <bits/stdc++.h>

#define fl(i,m,n) for(int (i) = (m); (i) < (n); ++(i))

using namespace std;

typedef long long ll;

int main() {

	int n;
	cin >> n;
	cin.get();
	fl (i, 0, n) {
		string s;
		getline(cin, s);
		cout << s << " " << 42 << endl;
	}

    return 0;
}
